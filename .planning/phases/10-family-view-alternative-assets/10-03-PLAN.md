---
phase: "10"
plan: "03"
type: execute
wave: 3
depends_on: ["10-02"]
files_modified: ["frontend/src/components/Family/FamilySelector.tsx", "frontend/src/components/Dashboard/StatsGrid.tsx", "frontend/tests/family-view.spec.ts"]
autonomous: false
requirements: ["V3-FAM-02", "V3-FAM-03"]
must_haves:
  truths:
    - "User can switch between Consolidated Family View and individual Member Views"
    - "Dashboard Stats Grid shows Post-Tax Total Value and Family XIRR in Consolidated mode"
    - "User can rename a Managed Profile (e.g., from PAN to 'Spouse')"
  artifacts:
    - path: "frontend/src/components/Family/FamilySelector.tsx"
      provides: "UI for switching profiles and renaming members"
    - path: "frontend/src/components/Dashboard/StatsGrid.tsx"
      provides: "Aggregated family metrics display"
    - path: "frontend/tests/family-view.spec.ts"
      provides: "E2E verification of the family flow"
---

<objective>
Integrate the multi-member logic into the frontend, allowing users to switch views, rename family members, and see consolidated family performance.

Purpose: Support D-03 (Member Naming), D-05 (Global Stats Update), and REQ-10.2 (Member Assignment UI).
Output: User-facing family dashboard.
</objective>

<execution_context>
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-family-view-alternative-assets/10-02-PLAN.md
@frontend/src/components/Dashboard/StatsGrid.tsx
@frontend/src/components/Family/FamilyManager.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Family Selector & Profile Renaming</name>
  <files>frontend/src/components/Family/FamilySelector.tsx</files>
  <read_first>frontend/src/components/Family/FamilyManager.tsx</read_first>
  <action>
    Create `frontend/src/components/Family/FamilySelector.tsx`:
    1. Fetch managed profiles from the backend (updated in Plan 02).
    2. Provide a list of profiles with a "Consolidated View" option at the top.
    3. Allow the user to select a profile (update `selectedProfileId` in global state/context).
    4. Implement an inline "Rename" feature for profiles that currently show just a PAN (Per D-03).
    5. Wire the selector into the Dashboard layout (e.g., in the sidebar or a sub-header).
    
    Per 10-UI-SPEC: Use 'Secondary Focus' for Family Member list.
  </action>
  <acceptance_criteria>
    The selector correctly updates the dashboard view based on selection and allows renaming of PAN-based profiles.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit FamilySelector</automated>
  </verify>
  <done>Family Selector UI implemented with renaming capability.</done>
</task>

<task type="auto">
  <name>Task 2: Update Stats Grid for Consolidated Metrics</name>
  <files>frontend/src/components/Dashboard/StatsGrid.tsx</files>
  <read_first>frontend/src/components/Dashboard/StatsGrid.tsx</read_first>
  <action>
    Modify `frontend/src/components/Dashboard/StatsGrid.tsx`:
    1. Update the overall summary cards to handle the consolidated metrics returned by the backend.
    2. Add/Update cards for "Post-Tax Total Value" and "Overall Post-Tax XIRR" (Per D-05).
    3. Ensure labels change appropriately (e.g., "Family Total" vs "Individual Total") based on the current selection.
    
    Per D-06: Maintain symmetry with Pre-Tax values for comparison.
  </action>
  <acceptance_criteria>
    The Stats Grid displays aggregated family metrics when 'Consolidated View' is active and individual metrics otherwise.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit StatsGrid</automated>
  </verify>
  <done>Dashboard Stats Grid updated for family aggregation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full Family View integration (Multi-PAN Splitting -> Managed Profiles -> Consolidated UI).</what-built>
  <how-to-verify>
    1. Start the app: `npm run dev`.
    2. Upload a multi-PAN CAS statement (if available) or mock one.
    3. Verify that two or more family members appear in the sidebar/selector.
    4. Switch to "Consolidated View" and check the Stats Grid for combined totals.
    5. Rename a profile from its PAN to "Test Member" and verify persistence.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Add E2E tests for Family Flow</name>
  <files>frontend/tests/family-view.spec.ts</files>
  <read_first>frontend/tests/smoke.spec.ts</read_first>
  <action>
    Create a Playwright E2E test `frontend/tests/family-view.spec.ts`:
    1. Mock a successful CAS upload with multiple PANs.
    2. Verify the presence of multiple member profiles in the UI.
    3. Toggle between Consolidated and Individual views.
    4. Verify that total values in the Stats Grid change correctly between views.
  </action>
  <acceptance_criteria>
    E2E test passes locally, confirming the end-to-end family splitting and reporting flow.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test frontend/tests/family-view.spec.ts</automated>
  </verify>
  <done>E2E coverage added for Family View features.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Frontend -> API | Selection and renaming requests must be validated by the backend (already mitigated in Plan 01/02). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Spoofing | FamilySelector | mitigate | Ensure frontend only displays profiles returned by the authorized `/managed-profiles` call. |
</threat_model>

<verification>
Manual verification of the UI flow followed by running the new Playwright E2E test.
</verification>

<success_criteria>
Users can successfully track and manage multiple family members from a single dashboard with consolidated post-tax performance metrics.
</success_criteria>

<output>
Create `.planning/phases/10-family-view-alternative-assets/10-03-SUMMARY.md` when done
</output>
