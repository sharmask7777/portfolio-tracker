---
phase: 10
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified: [frontend/src/components/FamilySelector.tsx, frontend/src/components/FamilySelector.test.tsx, frontend/src/components/StatsGrid.tsx, frontend/src/components/StatsGrid.test.tsx, frontend/src/App.tsx, frontend/tests/family-view.spec.ts]
autonomous: true
requirements: [V3-FAM-02, V3-FAM-03]
must_haves:
  truths:
    - "Family member selector allows switching between profiles and consolidated view"
    - "Stats grid displays both pre-tax and post-tax metrics for comparison"
    - "User can rename a managed profile via the UI"
  artifacts:
    - path: "frontend/src/components/FamilySelector.tsx"
      provides: "Profile navigation"
    - path: "frontend/src/components/StatsGrid.tsx"
      provides: "Aggregated metric visualization"
    - path: "frontend/tests/family-view.spec.ts"
      provides: "E2E verification of family dashboard"
---

<objective>
Implement the Family Dashboard UI with profile selection and symmetric pre/post-tax metrics.
Purpose: Provide a unified view of family wealth with easy navigation and clear tax impact visibility.
Output: FamilySelector, StatsGrid components, and E2E test suite.
</objective>

<execution_context>
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-family-view-alternative-assets/10-CONTEXT.md
@.planning/phases/10-family-view-alternative-assets/10-UI-SPEC.md
@frontend/src/App.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FamilySelector Component & Navigation</name>
  <files>frontend/src/components/FamilySelector.tsx, frontend/src/components/FamilySelector.test.tsx</files>
  <read_first>frontend/src/App.tsx</read_first>
  <behavior>
    - FamilySelector should list "Consolidated" and all detected "Managed Profiles".
    - Selecting a profile should trigger a refresh of the dashboard with that profile's data.
  </behavior>
  <action>
    Implement the profile selection UI (REQ-10.2).
    1. Create `frontend/src/components/FamilySelector.test.tsx`. Test that clicking a profile emits the correct `onSelect` event.
    2. Create `frontend/src/components/FamilySelector.tsx`. Implement a horizontal scrollable list of profiles or a dropdown (per UI-SPEC spacing).
    3. Include an "Edit" icon next to managed profiles to trigger renaming.
  </action>
  <verify>
    <automated>npm run test:unit FamilySelector</automated>
  </verify>
  <acceptance_criteria>FamilySelector component is created with passing unit tests.</acceptance_criteria>
  <done>Profile navigation component is implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StatsGrid with Pre/Post Tax Symmetry</name>
  <files>frontend/src/components/StatsGrid.tsx, frontend/src/components/StatsGrid.test.tsx</files>
  <read_first>frontend/src/App.tsx</read_first>
  <behavior>
    - StatsGrid should show: Total Value, Total Invested, Overall XIRR/Returns.
    - Each metric should have a secondary smaller label showing the Post-Tax equivalent (Symmetry, D-06).
  </behavior>
  <action>
    Create a reusable stats display with tax-aware metrics (D-05, D-06).
    1. Create `frontend/src/components/StatsGrid.test.tsx`. Verify that both `totalValue` and `postTaxTotalValue` are rendered when props are provided.
    2. Create `frontend/src/components/StatsGrid.tsx`. Extract the stats cards logic from `App.tsx` into this component.
    3. Update the layout to show Pre-Tax as the primary number and Post-Tax as a secondary "After Tax" label below it (per D-06).
  </action>
  <verify>
    <automated>npm run test:unit StatsGrid</automated>
  </verify>
  <acceptance_criteria>StatsGrid component displays symmetric metrics with passing unit tests.</acceptance_criteria>
  <done>Metrics visualization supports pre/post tax comparison.</done>
</task>

<task type="auto">
  <name>Task 3: Dashboard Integration & E2E Verification</name>
  <files>frontend/src/App.tsx, frontend/tests/family-view.spec.ts</files>
  <read_first>frontend/src/App.tsx</read_first>
  <action>
    Integrate all components and verify the full user flow (V3-FAM-03).
    1. Create `frontend/tests/family-view.spec.ts`. Define an E2E test that uploads a multi-PAN CAS, verifies multiple profiles appear in the selector, switches between them, and checks for post-tax labels in the stats grid.
    2. Update `frontend/src/App.tsx`:
       - Integrate `FamilySelector` at the top of the dashboard.
       - Use `StatsGrid` for the summary cards.
       - Implement the profile renaming modal/popover logic (using `PATCH /api/family/profile/:id`).
       - Ensure `userId` and `profileId` are passed to the backend `/summary` call based on selection.
  </action>
  <verify>
    <automated>npx playwright test frontend/tests/family-view.spec.ts</automated>
  </verify>
  <acceptance_criteria>E2E test passes, confirming end-to-end family view functionality.</acceptance_criteria>
  <done>Family dashboard is fully integrated and verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User -> UI | Profile renaming must handle long strings and special characters safely. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Information Disclosure | UI | mitigate | Ensure profile names are HTML-escaped to prevent XSS. |
| T-10-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED] packages. |
</threat_model>

<verification>
Run unit tests for components and full E2E suite for the family view.
</verification>

<success_criteria>
Family view is fully operational on the frontend with verified navigation and symmetric metrics.
</success_criteria>

<output>
Create `.planning/phases/10-family-view-alternative-assets/10-03-SUMMARY.md` when done
</output>
