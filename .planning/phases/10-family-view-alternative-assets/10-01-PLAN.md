---
phase: 10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/prisma/schema.prisma, backend/src/services/family.service.ts, backend/src/test/family.profile.test.ts, backend/src/test/performance.aggregate.test.ts]
autonomous: true
requirements: [V3-FAM-01, V3-FAM-02]
must_haves:
  truths:
    - "ManagedProfile model exists in database"
    - "FamilyService can create and retrieve ManagedProfiles for a user"
    - "Performance aggregation tests pass"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "ManagedProfile data model"
    - path: "backend/src/services/family.service.ts"
      provides: "CRUD for managed profiles"
    - path: "backend/src/test/performance.aggregate.test.ts"
      provides: "Validation for multi-portfolio performance calculation"
---

<objective>
Establish the backend foundation for Managed Profiles and verify multi-portfolio aggregation logic.
Purpose: Enable tracking different family members under a single user account and ensure calculation correctness.
Output: Schema migration, FamilyService extensions, and performance test suite.
</objective>

<execution_context>
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-family-view-alternative-assets/10-CONTEXT.md
@.planning/phases/10-family-view-alternative-assets/10-RESEARCH.md
@backend/prisma/schema.prisma
@backend/src/services/family.service.ts
@backend/src/services/performance.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Schema for Managed Profiles</name>
  <files>backend/prisma/schema.prisma</files>
  <read_first>backend/prisma/schema.prisma</read_first>
  <action>
    Add the `ManagedProfile` model and update `Portfolio` to link to it (per D-01, D-02).
    - `ManagedProfile`: { id (UUID), name (String), pan (String), userId (String, FK to User), portfolios (Portfolio[]), createdAt, updatedAt }
    - `Portfolio`: Add optional `managedProfileId` (String) and `managedProfile` relation.
    - Ensure `@@unique([userId, pan])` on `ManagedProfile` to prevent duplicate profiles for the same member.
    Run `npx prisma migrate dev --name add_managed_profiles` or equivalent to apply changes.
  </action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <acceptance_criteria>Prisma schema validates and migration file is created.</acceptance_criteria>
  <done>ManagedProfile model is persisted in the database schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Managed Profile CRUD (Service + Unit Tests)</name>
  <files>backend/src/services/family.service.ts, backend/src/test/family.profile.test.ts</files>
  <read_first>backend/src/services/family.service.ts</read_first>
  <behavior>
    - FamilyService.createManagedProfile(userId, pan, name): Should create a new profile.
    - FamilyService.getManagedProfiles(userId): Should return all profiles for a user.
    - FamilyService.updateManagedProfileName(userId, profileId, name): Should update name if user owns profile.
  </behavior>
  <action>
    Implement Managed Profile management in `FamilyService`.
    1. Create `backend/src/test/family.profile.test.ts`. Test CRUD for ManagedProfile via FamilyService, ensuring `userId` isolation.
    2. Update `backend/src/services/family.service.ts` to implement `createManagedProfile`, `getManagedProfiles`, and `updateManagedProfileName`.
    3. Ensure `FamilyService.getOrCreateManagedProfile(userId, pan)` exists for auto-splitting use (Task 10-02-01).
  </action>
  <verify>
    <automated>npm test backend/src/test/family.profile.test.ts</automated>
  </verify>
  <acceptance_criteria>All CRUD operations for ManagedProfile work as expected in isolation.</acceptance_criteria>
  <done>Managed Profile service logic is implemented and verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Family Performance Aggregation (Service updates + Unit Tests)</name>
  <files>backend/src/services/performance.service.ts, backend/src/test/performance.aggregate.test.ts</files>
  <read_first>backend/src/services/performance.service.ts</read_first>
  <behavior>
    - Performance Aggregation: calculateXIRR should handle a combined list of transactions from multiple portfolios correctly.
    - Verification: Combined XIRR of Portfolios A and B equals XIRR of (A+B) cashflows.
  </behavior>
  <action>
    Ensure `PerformanceService` and related helpers support multi-portfolio aggregation (per D-05).
    1. Create `backend/src/test/performance.aggregate.test.ts`. Test that `PerformanceService.calculateXIRR` correctly aggregates cashflows from two portfolios (A and B) and returns the combined XIRR when given a total value.
    2. If needed, update `PerformanceService` to provide a helper for merging `Transaction` arrays from different folios/portfolios into a single timeline for the solver.
  </action>
  <verify>
    <automated>npm test backend/src/test/performance.aggregate.test.ts</automated>
  </verify>
  <acceptance_criteria>Performance aggregation logic is mathematically verified for multi-portfolio scenarios.</acceptance_criteria>
  <done>Aggregate performance calculation is verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API -> DB | ManagedProfile creation must be scoped to the authenticated `userId`. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | FamilyService | mitigate | Ensure `userId` is strictly enforced in all ManagedProfile CRUD operations. |
| T-10-02 | Information Disclosure | FamilyService | mitigate | Unique constraint on `[userId, pan]` prevents one user from discovering another's profile names via PAN. |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Run backend unit tests for Managed Profiles and Performance aggregation.
</verification>

<success_criteria>
Managed Profiles are established in the backend with verified CRUD and aggregation logic.
</success_criteria>

<output>
Create `.planning/phases/10-family-view-alternative-assets/10-01-SUMMARY.md` when done
</output>
