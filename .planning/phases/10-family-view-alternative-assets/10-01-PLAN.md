---
phase: "10"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["backend/prisma/schema.prisma", "backend/src/services/family.service.ts", "backend/src/test/family.profile.test.ts"]
autonomous: true
requirements: ["V3-FAM-01", "V3-FAM-02"]
must_haves:
  truths:
    - "ManagedProfile entity exists in the database linked to a User"
    - "Portfolios can be associated with a specific ManagedProfile"
    - "Managed Profiles can be created and renamed via FamilyService"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "ManagedProfile model and Portfolio relationship"
    - path: "backend/src/services/family.service.ts"
      provides: "CRUD logic for Managed Profiles"
    - path: "backend/src/test/family.profile.test.ts"
      provides: "Verification of profile management logic"
---

<objective>
Establish the data foundation for multi-member tracking by introducing Managed Profiles. These profiles allow a single user to group portfolios by PAN (e.g., "Self", "Spouse") without requiring separate accounts.

Purpose: Support D-01 (Managed Profiles) and REQ-10.2 (Managed Profile CRUD).
Output: Updated schema and service layer for profile management.
</objective>

<execution_context>
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/milestones/v3.0-REQUIREMENTS.md
@backend/prisma/schema.prisma
@backend/src/services/family.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Prisma Schema for Managed Profiles</name>
  <files>backend/prisma/schema.prisma</files>
  <read_first>backend/prisma/schema.prisma</read_first>
  <action>
    Modify `backend/prisma/schema.prisma` to:
    1. Add `ManagedProfile` model with fields: `id` (UUID), `name` (String), `pan` (String), `userId` (String), `createdAt`, `updatedAt`.
    2. Add `@@unique([userId, pan])` constraint to `ManagedProfile` to prevent duplicate profiles for the same PAN under one user.
    3. Add `managedProfileId` (String?) to `Portfolio` model and set up the relation to `ManagedProfile`.
    4. Add `managedProfiles` relation to `User` model.
    5. Run `npx prisma generate` to update the client.
    
    Per D-01 and D-03: Profiles are identified by PAN and belong to a User.
  </action>
  <acceptance_criteria>
    `npx prisma validate` passes and the generated Prisma client includes the `ManagedProfile` model and the new `Portfolio` relation.
  </acceptance_criteria>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <done>Schema updated and validated with ManagedProfile model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Managed Profile logic in FamilyService</name>
  <files>backend/src/services/family.service.ts, backend/src/test/family.profile.test.ts</files>
  <read_first>backend/src/services/family.service.ts</read_first>
  <behavior>
    - Test 1: `upsertManagedProfile` creates a new profile if it doesn't exist for a given PAN.
    - Test 2: `upsertManagedProfile` returns existing profile if PAN matches for the same user.
    - Test 3: `renameProfile` updates the display name of a ManagedProfile.
    - Test 4: `getManagedProfiles` returns all profiles for a user.
  </behavior>
  <action>
    1. Create `backend/src/test/family.profile.test.ts` with the described behaviors.
    2. Update `backend/src/services/family.service.ts` to implement:
       - `upsertManagedProfile(userId: string, pan: string, name?: string)`: Uses `prisma.managedProfile.upsert`.
       - `renameProfile(profileId: string, userId: string, newName: string)`: Updates name ensuring user ownership.
       - `getManagedProfiles(userId: string)`: Lists all managed profiles for the user.
    
    Per D-03: Profiles initially identified by PAN, renaming allowed.
  </action>
  <acceptance_criteria>
    All tests in `family.profile.test.ts` pass and handle edge cases like unauthorized renaming.
  </acceptance_criteria>
  <verify>
    <automated>npm test backend/src/test/family.profile.test.ts</automated>
  </verify>
  <done>FamilyService extended with Managed Profile management logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API -> DB | ManagedProfile and Portfolio relations must be scoped by userId to prevent profile-hijacking. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Elevation of Privilege | renameProfile | mitigate | Ensure `userId` check in update query to prevent renaming profiles owned by other users. |
| T-10-02 | Information Disclosure | getManagedProfiles | mitigate | Filter by `userId` in all profile retrieval queries. |
</threat_model>

<verification>
Run `npx prisma validate` and `npm test backend/src/test/family.profile.test.ts`.
</verification>

<success_criteria>
Database supports Managed Profiles and the service layer can manage them (CRUD) with proper user scoping.
</success_criteria>

<output>
Create `.planning/phases/10-family-view-alternative-assets/10-01-SUMMARY.md` when done
</output>
