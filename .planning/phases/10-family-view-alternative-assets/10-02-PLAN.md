---
phase: 10
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified: [backend/src/services/sync.service.ts, backend/src/routes/portfolio.routes.ts, backend/src/routes/family.routes.ts, backend/src/test/sync.split.test.ts]
autonomous: true
requirements: [V3-FAM-01, V3-FAM-02, V3-FAM-03]
must_haves:
  truths:
    - "Syncing a multi-PAN CAS creates separate portfolios for each PAN"
    - "Portfolio summary supports filtering by profileId"
    - "Managed profiles can be renamed via PATCH API"
  artifacts:
    - path: "backend/src/test/sync.split.test.ts"
      provides: "Validation for CAS splitting logic"
    - path: "backend/src/services/sync.service.ts"
      provides: "Multi-PAN splitting logic"
    - path: "backend/src/routes/portfolio.routes.ts"
      provides: "Aggregated post-tax metrics API with filtering"
    - path: "backend/src/routes/family.routes.ts"
      provides: "Managed profile management endpoints"
---

<objective>
Implement CAS splitting by PAN, profile renaming API, and enhance the summary API with filtering and post-tax metrics.
Purpose: Automatically group family assets and provide tax-aware performance tracking with individual member drill-down.
Output: Updated SyncService, family management routes, summary route, and splitting integration tests.
</objective>

<execution_context>
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-family-view-alternative-assets/10-CONTEXT.md
@.planning/phases/10-family-view-alternative-assets/10-RESEARCH.md
@backend/src/services/sync.service.ts
@backend/src/services/tax.service.ts
@backend/src/routes/portfolio.routes.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Multi-PAN CAS Splitting</name>
  <files>backend/src/services/sync.service.ts, backend/src/test/sync.split.test.ts</files>
  <read_first>backend/src/services/sync.service.ts</read_first>
  <behavior>
    - Syncing a CAS with folios having different PANs should result in those folios being assigned to different Portfolios.
    - Each Portfolio should be linked to the correct ManagedProfile (auto-created if missing, per D-02).
  </behavior>
  <action>
    Modify `SyncService` to handle multi-PAN statements (REQ-10.1).
    1. Create `backend/src/test/sync.split.test.ts`. Define a test case with mocked CAS data containing two folios with different PANs. Verify that after `SyncService.syncPortfolio`, two distinct portfolios exist, each linked to a `ManagedProfile` with the corresponding PAN.
    2. Modify `backend/src/services/sync.service.ts`:
       - During the folio loop, extract the PAN for the *current* folio (per REQ-10.1).
       - Use `FamilyService.getOrCreateManagedProfile(userId, pan)` to find/create the profile.
       - Use `prisma.portfolio.findFirst` or `upsert` to find/create a portfolio linked to that `managedProfileId` and `userId`.
       - Assign the folio to THIS portfolio instead of the default one.
  </action>
  <verify>
    <automated>npm test backend/src/test/sync.split.test.ts</automated>
  </verify>
  <acceptance_criteria>Integration test for multi-PAN splitting passes, creating separate portfolios and managed profiles.</acceptance_criteria>
  <done>CAS splitting logic is implemented and verified.</done>
</task>

<task type="auto">
  <name>Task 2: API Route for Managed Profile Renaming</name>
  <files>backend/src/routes/family.routes.ts</files>
  <read_first>backend/src/services/family.service.ts</read_first>
  <action>
    Create a new route file `backend/src/routes/family.routes.ts` (or update existing) to expose profile management (per Revision Fix 1).
    1. Implement `PATCH /api/family/profile/:id` which accepts `{ name: string }`.
    2. The handler MUST verify that the authenticated `userId` owns the `ManagedProfile` before updating.
    3. Register this route in the main app router.
  </action>
  <verify>
    <automated>curl -X PATCH -H "Content-Type: application/json" -d '{"name": "Spouse"}' "http://localhost:3001/api/family/profile/mock-id"</automated>
  </verify>
  <acceptance_criteria>PATCH route updates the profile name and returns 200, or 403/404 if unauthorized/missing.</acceptance_criteria>
  <done>Managed Profile renaming API is available.</done>
</task>

<task type="auto">
  <name>Task 3: Enhanced Summary API with Filtering and Post-Tax Metrics</name>
  <files>backend/src/routes/portfolio.routes.ts</files>
  <read_first>backend/src/routes/portfolio.routes.ts</read_first>
  <action>
    Update the `/summary` route in `backend/src/routes/portfolio.routes.ts` to support member filtering and explicit tax-adjusted metrics (D-05, REQ-10.4, Revision Fix 2).
    1. Accept optional `profileId` (or `managedProfileId`) in query params.
    2. If `profileId` is provided, filter the portfolios being aggregated to only those linked to that profile.
    3. Ensure `taxSlab` is correctly parsed from query params.
    4. In the aggregation loop, explicitly calculate `totalEstimatedTax` across all (filtered) folios using `TaxService.calculateUnrealizedTax`.
    5. Calculate `postTaxTotalValue = totalValue - totalEstimatedTax` (D-05).
    6. Calculate `overallPostTaxXirr` using `PerformanceService.calculateXIRR` with aggregated transactions and `postTaxTotalValue`.
    7. Update the JSON response `metrics` object to include `postTaxTotalValue`, `postTaxXirr`, and `estimatedTax`.
  </action>
  <verify>
    <automated>curl -s "http://localhost:3001/api/portfolio/summary?profileId=mock-id&taxSlab=0.3" | jq '.metrics'</automated>
  </verify>
  <acceptance_criteria>Summary API returns filtered post-tax metrics when a profileId is provided.</acceptance_criteria>
  <done>Backend summary response includes explicit tax-adjusted metrics and supports member-level filtering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API -> Client | Post-tax metrics must not leak PII; they aggregate values only. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Elevation of Privilege | SyncService | mitigate | Ensure ManagedProfiles are always linked to the requesting `userId`. |
| T-10-04 | Information Disclosure | Summary Route | mitigate | Validate that `profileId` belongs to the requesting user before returning aggregated data. |
</threat_model>

<verification>
Run backend integration tests and verify summary API output via robust curl commands.
</verification>

<success_criteria>
CAS splitting, profile renaming, and filtered post-tax aggregation are fully implemented in the backend.
</success_criteria>

<output>
Create `.planning/phases/10-family-view-alternative-assets/10-02-SUMMARY.md` when done
</output>
