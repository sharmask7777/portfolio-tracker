---
phase: "10"
plan: "02"
type: execute
wave: 2
depends_on: ["10-01"]
files_modified: ["backend/src/services/sync.service.ts", "backend/src/routes/portfolio.routes.ts", "backend/src/test/sync.split.test.ts"]
autonomous: true
requirements: ["V3-FAM-01", "V3-FAM-03"]
must_haves:
  truths:
    - "A single CAS upload containing multiple PANs results in separate portfolios and profiles"
    - "Portfolio summary API supports 'Consolidated' view for all user portfolios"
    - "Portfolio summary API supports filtering by specific Managed Profile"
    - "Aggregated metrics (XIRR) account for all cashflows in the family"
  artifacts:
    - path: "backend/src/services/sync.service.ts"
      provides: "Multi-PAN splitting logic during ingestion"
    - path: "backend/src/routes/portfolio.routes.ts"
      provides: "API endpoints for family-aware portfolio summary"
    - path: "backend/src/test/sync.split.test.ts"
      provides: "Integration test for CAS splitting"
---

<objective>
Implement the core logic for automatically splitting CAS uploads by PAN and calculating aggregated performance metrics across the entire family.

Purpose: Support D-02 (Auto-Splitting), D-04/D-05 (Aggregated Metrics), and REQ-10.1 (Auto-split).
Output: Family-aware ingestion and reporting services.
</objective>

<execution_context>
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-family-view-alternative-assets/10-01-PLAN.md
@backend/src/services/sync.service.ts
@backend/src/routes/portfolio.routes.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update SyncService for Multi-PAN Splitting</name>
  <files>backend/src/services/sync.service.ts, backend/src/test/sync.split.test.ts</files>
  <read_first>backend/src/services/sync.service.ts</read_first>
  <behavior>
    - Test 1: Given a mock CAS data with PAN-A and PAN-B, `SyncService` creates two ManagedProfiles.
    - Test 2: Folios from PAN-A are assigned to a Portfolio linked to Profile-A.
    - Test 3: Folios from PAN-B are assigned to a Portfolio linked to Profile-B.
    - Test 4: Subsequent uploads to the same PANs do not duplicate portfolios.
  </behavior>
  <action>
    1. Create `backend/src/test/sync.split.test.ts` with mock multi-PAN CAS data.
    2. Modify `backend/src/services/sync.service.ts`:
       - Loop through `parsedData.folios`.
       - For each folio, extract PAN (use `folioData.pan` or fallback to `investor_info.pan`).
       - Call `FamilyService.upsertManagedProfile` to get/create a profile for that PAN.
       - Find/Create a `Portfolio` linked to that `ManagedProfile` and the current `User`.
       - Assign the `Folio` to that specific `Portfolio`.
       
    Per D-02: Auto-splitting based on unique PANs detected in CAS.
  </action>
  <acceptance_criteria>
    `sync.split.test.ts` passes, confirming that folios are correctly partitioned into profile-specific portfolios.
  </acceptance_criteria>
  <verify>
    <automated>npm test backend/src/test/sync.split.test.ts</automated>
  </verify>
  <done>SyncService splits CAS data by PAN into separate portfolios.</done>
</task>

<task type="auto">
  <name>Task 2: Update Portfolio Summary API for Family Metrics</name>
  <files>backend/src/routes/portfolio.routes.ts</files>
  <read_first>backend/src/routes/portfolio.routes.ts</read_first>
  <action>
    Update the `GET /api/portfolio/summary` route in `backend/src/routes/portfolio.routes.ts`:
    1. Accept `managedProfileId` query param.
    2. If `managedProfileId` is provided, filter portfolios by that profile.
    3. If no ID is provided, fetch ALL portfolios for the user (Consolidated View).
    4. Ensure the aggregated metrics (totalValue, totalInvested, overallXirr) are calculated across all portfolios in the current selection.
    5. Update `postTaxXirr` calculation to use the combined cashflows of all selected portfolios (Per D-05).
    6. Return the list of `ManagedProfiles` for the user in the response to assist frontend navigation.
  </action>
  <acceptance_criteria>
    The summary API returns aggregated metrics for all portfolios by default, and allows filtering by a specific Managed Profile ID.
  </acceptance_criteria>
  <verify>
    <automated>curl "http://localhost:3001/api/portfolio/summary" (mocked userId needed)</automated>
  </verify>
  <done>Summary API supports Consolidated View and Managed Profile filtering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CAS PDF -> Backend | `casparser` results are trusted, but portfolio assignment must be strictly bound to the authenticated user. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | syncPortfolio | mitigate | Ensure portfolios are created with the authenticated `userId` even when split by PAN. |
| T-10-04 | Information Disclosure | summary route | mitigate | Validate that the requested `managedProfileId` belongs to the requesting `userId`. |
</threat_model>

<verification>
Run `npm test backend/src/test/sync.split.test.ts` and verify `/summary` endpoint behavior with multiple portfolios.
</verification>

<success_criteria>
The system correctly partitions data by member and provides aggregated family-level performance reporting.
</success_criteria>

<output>
Create `.planning/phases/10-family-view-alternative-assets/10-02-SUMMARY.md` when done
</output>
