---
phase: 9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  frontend/src/components/Tax/TaxView.tsx,
  frontend/src/components/Tax/SimulationModal.tsx,
  frontend/src/App.tsx
]
autonomous: true
requirements: [V2-DYN-01, V2-TAX-01]

must_haves:
  truths:
    - "Clicking a scheme in the Tax Harvesting card opens the SimulationModal pre-filled."
    - "SimulationModal triggers analysis immediately when opened via harvesting click."
    - "Post-Tax XIRR/Return column shows reduced opacity (0.5) while re-fetching data after a Tax Slab change."
  artifacts:
    - path: "frontend/src/components/Tax/TaxView.tsx"
      provides: "Harvesting click handlers"
    - path: "frontend/src/components/Tax/SimulationModal.tsx"
      provides: "Auto-simulation support via initialUnits prop"
    - path: "frontend/src/App.tsx"
      provides: "Global loading state and modal wiring"
  key_links:
    - from: "TaxView"
      to: "App.tsx (onSimulateHarvest)"
      via: "callback prop"
    - from: "App.tsx"
      to: "SimulationModal"
      via: "initialUnits prop"
---

<objective>
Integrated Tax Harvesting flow and UI polish for metric reactivity.

Purpose: Improve UX by connecting harvesting opportunities directly to the simulation tool and providing visual feedback during slab-triggered re-calculations.
Output: Updated frontend components with integrated flows and loading states.
</objective>

<execution_context>
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-milestone-v2.0-polish-verification/09-CONTEXT.md
@frontend/src/App.tsx
@frontend/src/components/Tax/TaxView.tsx
@frontend/src/components/Tax/SimulationModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrated Tax Harvesting Flow (D-01, D-02)</name>
  <files>frontend/src/components/Tax/TaxView.tsx, frontend/src/components/Tax/SimulationModal.tsx, frontend/src/App.tsx</files>
  <read_first>
    - `frontend/src/components/Tax/TaxView.tsx`: Identify the harvesting opportunity mapping (approx line 125).
    - `frontend/src/components/Tax/SimulationModal.tsx`: Check `handleSimulate` and `setAmount` logic.
  </read_first>
  <action>
    1.  **TaxView.tsx**:
        - Add `onSimulateHarvest: (folioId: string, units: number) => void` to props.
        - In the harvesting opportunity list, wrap items in a clickable `div` (or add a "Simulate" link) that calls `onSimulateHarvest`.
    2.  **SimulationModal.tsx**:
        - Add `initialUnits?: number` to props.
        - Add a `useEffect` that runs once on mount: if `initialUnits` is present and `currentPrice > 0`, set `amount` to `initialUnits * currentPrice` and trigger `handleSimulate()`.
    3.  **App.tsx**:
        - Add `simUnits` state (`const [simUnits, setSimUnits] = useState<number | null>(null)`).
        - Update `setSimFolio` calls to also `setSimUnits`.
        - Pass `onSimulateHarvest` to `TaxView` that finds the folio by ID from the current `portfolio.folios` and opens the modal.
        - Pass `initialUnits={simUnits}` to `SimulationModal`.
  </action>
  <verify>
    - Click a harvesting opportunity in the Tax tab.
    - Verify `SimulationModal` opens and "Analyzing..." appears immediately.
    - Verify results are displayed without manual "Analyze Tax" click.
  </verify>
  <acceptance_criteria>
    - Harvesting opportunities are interactive and trigger pre-filled simulation.
    - Simulation results for harvesting flow are accurate and automatic.
  </acceptance_criteria>
  <done>Harvesting flow integrated and auto-simulating.</done>
</task>

<task type="auto">
  <name>Task 2: Tax Slab Reactivity & Loading Visuals (D-04)</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - `frontend/src/App.tsx`: Locate `fetchSummary` and the holdings table in the 'overview' tab.
  </read_first>
  <action>
    1.  Add `isRefetching` state (default `false`).
    2.  Update `fetchSummary`: Set `isRefetching(true)` at start, `isRefetching(false)` at end.
    3.  In the holdings table (within the 'overview' tab):
        - Locate the `<td>` for "Post-Tax XIRR / Return".
        - Apply conditional styling: `style={{ opacity: isRefetching ? 0.5 : 1, transition: 'opacity 0.2s' }}`.
    4.  Verify that changing the Tax Slab in the header triggers this visual state during the API call.
  </action>
  <verify>
    - Change Tax Slab in the header.
    - Observe the "Post-Tax" column in the main holdings table.
    - Confirm it dims while data is loading.
  </verify>
  <acceptance_criteria>
    - "Post-Tax" column provides visual feedback during re-calculation.
    - UI remains responsive during background re-fetch.
  </acceptance_criteria>
  <done>Visual feedback for slab-triggered re-calculations implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Browser → Simulation API | Untrusted Folio ID and Units sent to backend for tax calculation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | `POST /api/tax/simulate-sell` | accept | Folio IDs are UUIDs; risk of guessing is low. Formal auth is out of scope for v2.0. |
| T-09-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- Integrated Harvesting Flow: Clicking a harvest suggestion correctly loads and runs the simulation.
- Reactivity: Changing tax slab updates both TaxView and the main table with correct visual feedback.
</verification>

<success_criteria>
- Seamless transition from "Harvesting Opportunity" to "Tax Simulation".
- Clear visual indication of "Post-Tax" metric re-calculation.
</success_criteria>

<output>
Create `.planning/phases/09-milestone-v2.0-polish-verification/09-01-PLAN.md` when done
</output>
