---
phase: 07-dynamic-returns-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend/src/contexts/SettingsContext.tsx, frontend/src/main.tsx, frontend/src/App.tsx]
autonomous: true
requirements: [V2-DYN-01]

must_haves:
  truths:
    - "Theme selection persists across page refreshes"
    - "SettingsContext provides performanceMode and theme to the entire app"
  artifacts:
    - path: "frontend/src/contexts/SettingsContext.tsx"
      provides: "Global state management for UI preferences"
    - path: "frontend/src/main.tsx"
      provides: "Context provider wrapper"
  key_links:
    - from: "frontend/src/contexts/SettingsContext.tsx"
      to: "localStorage"
      via: "JSON.parse/stringify"
---

<objective>
Setup the core infrastructure for global settings. Create a `SettingsContext` to manage both the existing Theme selection and the new Performance Metric preference, ensuring they persist in `localStorage`.
</objective>

<execution_context>
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@frontend/src/App.tsx
@frontend/src/main.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SettingsContext</name>
  <files>frontend/src/contexts/SettingsContext.tsx</files>
  <action>
    Create a new directory `frontend/src/contexts`.
    Implement `SettingsContext` with the following state:
    - `theme`: 'light' | 'dark' (default: 'light')
    - `performanceMode`: 'XIRR' | 'ABS' (default: 'XIRR', per D-04)
    
    Include functions to toggle theme and set performance mode.
    Use `useEffect` to:
    - Load initial state from `localStorage` (per D-05).
    - Sync state to `localStorage` when it changes.
    - Apply the `data-theme` attribute to `document.documentElement` whenever the theme changes (moved from App.tsx).
    
    Export a `SettingsProvider` and a `useSettings` custom hook.
  </action>
  <verify>
    <automated>test -d frontend/src/contexts && test -f frontend/src/contexts/SettingsContext.tsx</automated>
  </verify>
  <done>SettingsContext implemented with persistence and theme application logic.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap App with SettingsProvider</name>
  <files>frontend/src/main.tsx</files>
  <action>
    Import `SettingsProvider` from `./contexts/SettingsContext`.
    Wrap the `App` component with `SettingsProvider` inside `StrictMode`.
  </action>
  <verify>
    <automated>grep -q "SettingsProvider" frontend/src/main.tsx</automated>
  </verify>
  <done>App is wrapped with SettingsProvider in main.tsx.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor App.tsx to use SettingsContext</name>
  <files>frontend/src/App.tsx</files>
  <action>
    Remove local `theme` state and `toggleTheme` function.
    Import and use the `useSettings` hook to get `theme` and `toggleTheme`.
    Update the theme toggle button in the header to use the new `toggleTheme` from context.
    Verify that theme switching still works as expected.
  </action>
  <verify>
    <automated>! grep -q "const \[theme, setTheme\] = useState" frontend/src/App.tsx</automated>
  </verify>
  <done>App.tsx refactored to use global settings context for theme management.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → LocalStorage | Persistence of UI preferences |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | localStorage | mitigate | Validate loaded settings against expected values (union types) and fallback to defaults if corrupted. |
</threat_model>

<verification>
1. Verify `frontend/src/contexts/SettingsContext.tsx` exists.
2. Verify `main.tsx` has `SettingsProvider`.
3. Verify `App.tsx` no longer has local theme state.
4. Verify theme toggling persists after page reload.
</verification>

<success_criteria>
Theme selection is successfully moved to `SettingsContext` and persists across reloads.
</success_criteria>

<output>
Create `.planning/phases/07-dynamic-returns-ux/07-01-SUMMARY.md` when done
</output>
