---
phase: 07-dynamic-returns-ux
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: [frontend/src/App.tsx, frontend/src/App.css]
autonomous: true
requirements: [V2-DYN-01]

must_haves:
  truths:
    - "Segmented control [ XIRR | ABS ] is visible in the header"
    - "Switching to ABS updates Stats Grid card label to 'Total Return'"
    - "Switching to ABS updates Stats Grid card value to absolute return percentage"
    - "Switching to ABS updates Data Table 'XIRR' column to 'Return' with absolute return values"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "Reactive UI with metric toggle"
    - path: "frontend/src/App.css"
      provides: "Styles for segmented control"
---

<objective>
Implement the performance metric toggle in the global header and update the dashboard components (Stats Grid, Data Table, Insights) to reactively switch between XIRR and Absolute Return metrics.
</objective>

<execution_context>
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@frontend/src/App.tsx
@frontend/src/App.css
@frontend/src/contexts/SettingsContext.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add styles for Segmented Control</name>
  <files>frontend/src/App.css</files>
  <action>
    Add CSS classes for a segmented control (button group). 
    Requirements:
    - `.segmented-control`: Container with background, border-radius, and flex layout.
    - `.segmented-control button`: Style for individual options (transparent, font-weight).
    - `.segmented-control button.active`: Highlight the selected option (background-color, color, box-shadow).
    Ensure the styles match the existing design system (using CSS variables like `--bg-secondary`, `--accent-color`, etc.).
  </action>
  <verify>
    <automated>grep -q ".segmented-control" frontend/src/App.css</automated>
  </verify>
  <done>Styles for the metric toggle are added to App.css.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Metric Toggle in Header</name>
  <files>frontend/src/App.tsx</files>
  <action>
    Import `useSettings` and get `performanceMode` and `setPerformanceMode`.
    Add a segmented control in the `header` section, next to the theme toggle (per D-01).
    The control should have two buttons: "XIRR" and "ABS" (per D-02).
    Clicking a button should call `setPerformanceMode` with the corresponding value.
  </action>
  <verify>
    <automated>grep -q "setPerformanceMode" frontend/src/App.tsx</automated>
  </verify>
  <done>Global header toggle for performance metric is implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Dashboard for Reactive Metric Swapping</name>
  <files>frontend/src/App.tsx</files>
  <action>
    Refactor the following components/sections to use `performanceMode`:
    1. **Stats Grid:**
       - "Overall XIRR" card label should become "Total Return" if mode is 'ABS'.
       - The value should switch between `portfolio.metrics.xirr` and `portfolio.metrics.absoluteReturn`.
    2. **Data Table:**
       - Header "XIRR" should become "Return" (or "Abs. Return") if mode is 'ABS'.
       - Cell values should switch between `folio.metrics.xirr` and `folio.metrics.absoluteReturn`.
    3. **Key Insights:**
       - The text "Your top performing asset is... with an XIRR of..." should update its metric name and value based on the toggle.
    
    Ensure all values are formatted using `formatPercent`.
  </action>
  <verify>
    <automated>grep -q "performanceMode === 'XIRR'" frontend/src/App.tsx</automated>
  </verify>
  <done>Dashboard reactively updates labels and values based on the selected performance metric.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI → State | User interaction driving metric selection |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Information Disclosure | Stats Display | accept | User is choosing which calculation to view; no sensitive data leaked beyond what is already shown. |
</threat_model>

<verification>
1. Open the dashboard.
2. Toggle between XIRR and ABS in the header.
3. Observe that labels in Stats Grid and Table change instantly (D-06).
4. Observe that values in Stats Grid and Table change to reflect the selected metric.
5. Refresh the page and verify the selection is persisted (D-05).
</verification>

<success_criteria>
The dashboard successfully supports dynamic switching between XIRR and Absolute Return metrics with global persistence and consistent labeling.
</success_criteria>

<output>
Create `.planning/phases/07-dynamic-returns-ux/07-02-SUMMARY.md` when done
</output>
