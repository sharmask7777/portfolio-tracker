# Plan 12-01: Recharts AreaChart Integration

**Objective:** Solidify the base integration of the history chart, ensuring it correctly fetches data from the backend and adheres to the design system's color palette.

## Tasks
1. **Task: CSS Variable Sync**
   - **Files:** `frontend/src/components/Dashboard/HistoryChart.tsx`
   - **Action:** Update the `Area` and `linearGradient` colors in `HistoryChart.tsx` to strictly use `var(--accent-color)` for Current Value and `var(--text-secondary)` for Invested Amount. Ensure `fillOpacity` and `strokeWidth` match D-04/D-05.
   - **Verify:** Chart colors update when switching themes (light/dark).
2. **Task: API Connectivity & Error Handling**
   - **Files:** `frontend/src/components/Dashboard/HistoryChart.tsx`
   - **Action:** Ensure the `portfolioId` is correctly passed and the `axios` call to `${API_ENDPOINTS.PORTFOLIO}/${portfolioId}/history` is robust. Implement a specific "No History Data" empty state if the array is empty.
   - **Verify:** Manual check with empty portfolio.
