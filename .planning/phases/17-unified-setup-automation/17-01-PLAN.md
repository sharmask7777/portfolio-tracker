---
phase: 17-unified-setup-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [setup.sh]
autonomous: true
requirements: [DX-01]

must_haves:
  truths:
    - "Running setup.sh initializes all .env files from .env.example"
    - "setup.sh installs npm dependencies for both backend and frontend"
    - "setup.sh creates a python virtual environment and installs casparser in the backend"
  artifacts:
    - path: "setup.sh"
      provides: "Unified environment initialization"
    - path: "backend/.env"
      provides: "Local backend configuration"
    - path: "frontend/.env"
      provides: "Local frontend configuration"
  key_links:
    - from: "setup.sh"
      to: "backend/package.json"
      via: "npm install"
    - from: "setup.sh"
      to: "backend/venv"
      via: "python3 -m venv"
---

<objective>
Create a unified `setup.sh` script to simplify the environment initialization for new developers and users.

Purpose: Reduce friction in the onboarding process by automating environment variable setup and dependency installation.
Output: A cross-platform bash script `setup.sh`.
</objective>

<execution_context>
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-unified-setup-automation/17-CONTEXT.md
@.env.example
@backend/.env.example
@frontend/.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize setup.sh with environment variable management</name>
  <files>setup.sh</files>
  <action>
    Create a root `setup.sh` script that:
    1. Includes a shebang `#!/bin/bash`.
    2. Implements a helper function to copy `.env.example` to `.env` if the target doesn't exist.
    3. Applies this to the root, `backend/`, and `frontend/` directories.
    4. Adds basic logging to inform the user of progress (e.g., "Initializing environment variables...").
    5. Checks for basic pre-requisites: `node`, `npm`, and `python3`.
  </action>
  <verify>
    <automated>
      chmod +x setup.sh && ./setup.sh && test -f .env && test -f backend/.env && test -f frontend/.env
    </automated>
  </verify>
  <done>setup.sh exists and successfully creates .env files from templates.</done>
</task>

<task type="auto">
  <name>Task 2: Implement dependency installation and Prisma generation</name>
  <files>setup.sh</files>
  <action>
    Update `setup.sh` to handle local dependency setup:
    1. Backend:
       - Run `npm install`.
       - Run `npx prisma generate`.
       - Create a virtual environment at `backend/venv` using `python3 -m venv`.
       - Install `casparser` within the venv (use `backend/venv/bin/pip install casparser`).
    2. Frontend:
       - Run `npm install`.
    3. Ensure the script is idempotent (can be run multiple times safely).
  </action>
  <verify>
    <automated>
      ./setup.sh && test -d backend/node_modules && test -d frontend/node_modules && test -d backend/venv
    </automated>
  </verify>
  <done>setup.sh installs all Node.js and Python dependencies and generates the Prisma client.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> setup.sh | Script executes with user permissions on the host system. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-01 | Information Disclosure | .env files | mitigate | Ensure .env files are in .gitignore (verified). |
| T-17-01-02 | Tampering | setup.sh | mitigate | Use standard shell practices, avoid arbitrary remote code execution. |
| T-17-01-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED] packages. |
</threat_model>

<verification>
Run `./setup.sh` on a clean environment (or after deleting node_modules/venv/.env) and verify the app can be built/started locally.
</verification>

<success_criteria>
1. `setup.sh` is executable and idempotent.
2. All local dependencies are installed.
3. All `.env` files are initialized.
</success_criteria>

<output>
Create `.planning/phases/17-unified-setup-automation/17-01-SUMMARY.md` when done
</output>
