---
phase: 13-historical-insights-metrics
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified: [frontend/src/components/Dashboard/HistoricalHighlightsCard.tsx, frontend/src/App.tsx]
autonomous: false
requirements: [V4-INS-01, V4-INS-02]

must_haves:
  truths:
    - "Historical Highlights card is visible on the Overview dashboard"
    - "Card displays All-Time High Corpus and Max Invested with dates"
    - "Card includes a scrollable yearly breakdown of milestones"
    - "Metrics update when switching between family members/profiles (D-02)"
  artifacts:
    - path: "frontend/src/components/Dashboard/HistoricalHighlightsCard.tsx"
      provides: "Historical Highlights UI"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/Dashboard/HistoricalHighlightsCard.tsx"
      via: "Component instantiation in Overview tab"
---

<objective>
Create the 'Historical Highlights' dashboard card and integrate it into the main Overview tab. The card will display portfolio milestones (ATH, Max Invested) and a yearly breakdown.

Purpose: Surface long-term performance milestones to the user.
Output: New `HistoricalHighlightsCard` component integrated into `App.tsx`.
</objective>

<execution_context>
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-historical-insights-metrics/13-01-SUMMARY.md
@frontend/src/App.tsx
@frontend/src/components/Dashboard/StatsGrid.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HistoricalHighlightsCard component</name>
  <files>frontend/src/components/Dashboard/HistoricalHighlightsCard.tsx</files>
  <action>
    Create a new component `HistoricalHighlightsCard`.
    - Props: `portfolioId: string`.
    - Fetch data from `GET /api/portfolio/:portfolioId/stats` using `axios`.
    - Display "All-Time High Corpus" and "Maximum Invested" prominently at the top with dates.
    - Use icons like `Trophy` (for ATH) and `TrendingUp` (for Max Invested) per Claude's discretion.
    - Implement a scrollable table or list below the main metrics showing the yearly breakdown (ATH and Max Invested for the last 5 years).
    - Apply styling consistent with existing `Card` and `StatsGrid` components.
    - Handle loading and empty states.
  </action>
  <verify>
    Component renders correctly in isolation or with mock data.
  </verify>
  <done>HistoricalHighlightsCard component implemented with data fetching and yearly breakdown list.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate card into Dashboard Overview</name>
  <files>frontend/src/App.tsx</files>
  <action>
    Integrate `HistoricalHighlightsCard` into the `Overview` tab in `App.tsx`.
    - Place it in the `dashboard-sections` grid, ideally next to or below the History Chart.
    - Ensure it receives the current `portfolio.id` (which changes based on profile/family selection).
    - Per D-01 and D-02.
  </action>
  <verify>
    <automated>Check if component is rendered in the DOM when Overview tab is active.</automated>
  </verify>
  <done>HistoricalHighlightsCard is visible on the dashboard and updates when changing profiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Historical Highlights card with ATH and Max Invested milestones.</ what-built>
  <how-to-verify>
    1. Start frontend and backend.
    2. Visit the dashboard Overview tab.
    3. Verify the "Historical Highlights" card is visible.
    4. Check if ATH and Max Invested values/dates look reasonable.
    5. Scroll through the yearly breakdown list.
    6. Switch family members/profiles and verify metrics update.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> API | Fetching stats |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | Denial of Service | Frequent polling/re-fetching | accept | Normal dashboard usage pattern, no excessive polling. |
</threat_model>

<verification>
Verify UI responsiveness and visual consistency with the design system.
Verify that data re-fetches when `selectedProfileId` or `selectedFamilyId` changes.
</verification>

<success_criteria>
- Historical Highlights card implemented and integrated.
- Milestones displayed with dates.
- Yearly breakdown scrollable and accurate.
- Profile-aware updates functional.
</success_criteria>

<output>
Create `.planning/phases/13-historical-insights-metrics/13-02-SUMMARY.md` when done
</output>
