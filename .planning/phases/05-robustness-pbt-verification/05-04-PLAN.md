---
phase: 05-robustness-pbt-verification
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified: [backend/src/services/analytics.pbt.spec.ts]
autonomous: true
requirements: [PBT-FAM, PBT-XRAY]

must_haves:
  truths:
    - "Family aggregation math is consistent: sum of individual metrics equals aggregated metrics"
    - "X-Ray weighting percentages sum to 100% across all categories (Sector, Market Cap, Asset Allocation)"
    - "Arbitrage fund logic correctly handles weighting in exArbitrage views"
  artifacts:
    - path: "backend/src/services/analytics.pbt.spec.ts"
      provides: "Verification of aggregation and weighting logic"
---

<objective>
Verify Family aggregation math and X-Ray weighting percentages using Property-Based Testing.

Purpose: Ensure that consolidated views and deep analytics are accurate across multiple portfolios.
Output: Comprehensive PBT suite for Analytics and Aggregation.
</objective>

<execution_context>
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/src/services/xray.service.ts
@backend/src/services/family.service.ts
@backend/src/test/arbitraries.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify X-Ray Weighting and Normalization</name>
  <files>backend/src/services/analytics.pbt.spec.ts</files>
  <behavior>
    - Weight Sum: `sum(sectorPercentages) ≈ 100%`, `sum(assetAllocationPercentages) ≈ 100%`, `sum(marketCapPercentages) ≈ 100%`.
    - Ex-Arbitrage Consistency: Ex-arbitrage values should be less than or equal to total values for relevant categories.
    - Value Consistency: `percentage * totalValue ≈ categoryValue` for all entries.
  </behavior>
  <action>
    Create `backend/src/services/analytics.pbt.spec.ts`.
    Mock the `MarketDataService.getHoldings` and `MarketDataService.getLatestNAV` to return varied but controlled weights for different ISINs.
    Generate portfolios with multiple schemes and verify that the `getXRayData` aggregation logic maintains weighting invariants.
  </action>
  <verify>
    <automated>npm test backend/src/services/analytics.pbt.spec.ts</automated>
  </verify>
  <done>X-Ray weighting logic is verified and accurate.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Family Aggregation Math</name>
  <files>backend/src/services/analytics.pbt.spec.ts</files>
  <action>
    Implement properties to verify that when multiple portfolios are combined into a family view:
    - `TotalFamilyInvested = sum(PortfolioInvested)`
    - `TotalFamilyCurrentValue = sum(PortfolioCurrentValue)`
    - `TotalFamilyGain = sum(PortfolioGain)`
    - Verify that the weighted average XIRR of individual portfolios is reasonably close to the consolidated XIRR (accounting for time-weighting differences).
  </action>
  <verify>
    <automated>npm test backend/src/services/analytics.pbt.spec.ts</automated>
  </verify>
  <done>Family aggregation logic is mathematically consistent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External API -> Analytics | Mocking external data must be realistic enough to test weighting logic without being coupled to actual AMFI data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | Analytics Engine | mitigate | PBT ensures that floating point precision errors don't lead to "missing money" or >100% weights. |
</threat_model>

<verification>
Run the analytics PBT suite and ensure all weighting and aggregation invariants pass.
</verification>

<success_criteria>
Aggregation and X-Ray logic are verified as robust and accurate.
</success_criteria>

<output>
Create `.planning/phases/05-robustness-pbt-verification/05-04-SUMMARY.md` when done
</output>
