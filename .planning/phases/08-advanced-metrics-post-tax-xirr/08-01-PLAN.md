---
phase: 08-advanced-metrics-post-tax-xirr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/src/services/tax.service.ts, backend/src/services/performance.service.ts, backend/src/routes/portfolio.routes.ts, backend/src/services/tax.service.spec.ts]
autonomous: true
requirements: [V2-TAX-01, V2-TAX-02]
must_haves:
  truths:
    - "TaxService can calculate estimated tax liability on unrealized gains using a marginal slab."
    - "PerformanceService calculates post-tax metrics by adjusting the terminal value."
    - "Portfolio summary API returns postTaxXirr and postTaxAbsoluteReturn when taxSlab is provided."
  artifacts:
    - path: "backend/src/services/tax.service.ts"
      provides: "Unrealized tax estimation logic"
    - path: "backend/src/services/performance.service.ts"
      provides: "Post-tax performance calculation"
---

<objective>
Extend the backend services to calculate estimated tax liability on current holdings and derive post-tax performance metrics (XIRR and Absolute Return).
</objective>

<execution_context>
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@backend/src/services/tax.service.ts
@backend/src/services/performance.service.ts
@backend/src/routes/portfolio.routes.ts
@.planning/phases/08-advanced-metrics-post-tax-xirr/08-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend TaxService for Unrealized Tax Estimation</name>
  <files>backend/src/services/tax.service.ts, backend/src/services/tax.service.spec.ts</files>
  <behavior>
    - TaxService.getTaxRate should accept an optional taxSlab parameter (default 0) and return it if taxType is 'SLAB'.
    - New method TaxService.calculateUnrealizedTax(folioTransactions, currentPrice, assetType, assetName, taxSlab) should return total estimated tax.
    - Logic should use existing calculatePortfolioTax to get unrealized gain components (LTCG, STCG, SLAB) and apply respective rates.
    - LTCG rate (Equity post-2024): 12.5%, STCG rate (Equity): 20%.
    - Test Case 1: Debt fund with SLAB taxation at 30% slab.
    - Test Case 2: Equity fund with LTCG post-2024 (12.5%).
  </behavior>
  <action>
    - Update `getTaxRate` signature to include `taxSlab: number = 0`.
    - Update `getTaxRate` to return `taxSlab` when `taxType === 'SLAB'`.
    - Implement `calculateUnrealizedTax` helper:
      1. Call `calculatePortfolioTax` to get `TaxSummary`.
      2. For each unrealized component (stcg, ltcg, slab), calculate tax using `getTaxRate`.
      3. Return sum of estimated taxes.
    - Update `backend/src/services/tax.service.spec.ts` with tests for the new method.
  </action>
  <verify>
    <automated>npm test backend/src/services/tax.service.spec.ts</automated>
  </verify>
  <done>TaxService correctly estimates unrealized tax liability considering asset type and tax slab.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Post-Tax Performance Metrics</name>
  <files>backend/src/services/performance.service.ts, backend/src/services/performance.service.spec.ts</files>
  <behavior>
    - PerformanceMetrics interface should include optional postTaxXirr and postTaxAbsoluteReturn.
    - PerformanceService.getMetrics should accept an optional taxSlab.
    - If taxSlab is provided, calculate postTaxCurrentValue = currentValue - unrealizedTax.
    - Calculate postTaxXirr using adjusted terminal value.
    - Calculate postTaxAbsoluteReturn using adjusted terminal value.
  </behavior>
  <action>
    - Update `PerformanceMetrics` interface in `backend/src/services/performance.service.ts`.
    - Update `getMetrics` signature to include `options?: { taxSlab?: number, assetType?: AssetType, assetName?: string }`.
    - Inside `getMetrics`, if `options.taxSlab` is present:
      1. Call `TaxService.calculateUnrealizedTax` to get `estimatedTax`.
      2. Compute `postTaxCurrentValue = Math.max(0, currentValue - estimatedTax)`.
      3. Compute `postTaxXirr = this.calculateXIRR(normalizedTransactions, postTaxCurrentValue)`.
      4. Compute `postTaxAbsoluteReturn = this.calculateAbsoluteReturn(investedAmount, postTaxCurrentValue)`.
    - Update `backend/src/services/performance.service.spec.ts` with post-tax verification tests.
  </action>
  <verify>
    <automated>npm test backend/src/services/performance.service.spec.ts</automated>
  </verify>
  <done>PerformanceService returns post-tax metrics when tax slab information is provided.</done>
</task>

<task type="auto">
  <name>Task 3: Update Portfolio Summary API</name>
  <files>backend/src/routes/portfolio.routes.ts</files>
  <action>
    - In `router.get('/summary')`, extract `taxSlab` from `req.query` (parse as float, default 0.3 per D-06).
    - Pass `taxSlab` and asset info (type, name) in the `options` parameter to `PerformanceService.getMetrics`.
    - Update the `/summary` response to include these metrics for each folio and for the overall portfolio summary.
  </action>
  <verify>
    <automated>curl "http://localhost:3001/api/portfolio/summary?taxSlab=0.3" | grep -E "postTaxXirr|postTaxAbsoluteReturn"</automated>
  </verify>
  <done>The summary API returns post-tax metrics correctly based on the taxSlab query parameter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | User-provided taxSlab enters the system via query params. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Input Validation | portfolio.routes.ts | mitigate | Ensure taxSlab is a valid number between 0 and 1. |
| T-08-02 | DoS | PerformanceService | mitigate | XIRR solver has timeout/cap on iterations (node-irr default). |
</threat_model>

<verification>
Run backend tests: `npm test backend/src/services/tax.service.spec.ts backend/src/services/performance.service.spec.ts`
</verification>

<success_criteria>
- Backend calculates estimated tax liability for holdings.
- API returns post-tax XIRR and Absolute Return.
- All unit tests pass.
</success_criteria>

<output>
Create `.planning/phases/08-advanced-metrics-post-tax-xirr/08-01-SUMMARY.md` when done
</output>
