---
phase: 08-advanced-metrics-post-tax-xirr
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified: [frontend/src/contexts/SettingsContext.tsx, frontend/src/App.tsx]
autonomous: true
requirements: [V2-DYN-01, V2-TAX-01]
must_haves:
  truths:
    - "User can configure their marginal tax slab in the UI."
    - "Holdings table displays a Post-Tax column that reacts to the performance mode toggle."
    - "The UI fetches data from the backend using the configured tax slab."
  artifacts:
    - path: "frontend/src/contexts/SettingsContext.tsx"
      provides: "Tax slab state management"
---

<objective>
Update the frontend to allow users to configure their marginal tax slab and display post-tax performance metrics in the holdings table.
</objective>

<execution_context>
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@frontend/src/contexts/SettingsContext.tsx
@frontend/src/App.tsx
@.planning/phases/08-advanced-metrics-post-tax-xirr/08-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Tax Slab to SettingsContext</name>
  <files>frontend/src/contexts/SettingsContext.tsx</files>
  <action>
    - Add `taxSlab: number` to the `Settings` interface (default 0.3).
    - Add `setTaxSlab: (slab: number) => void` to the `SettingsContextType`.
    - Update `DEFAULT_SETTINGS` to include `taxSlab: 0.3`.
    - Update `isValidSettings` (or manual parsing logic) to handle `taxSlab`.
    - Implement `setTaxSlab` in the `SettingsProvider`.
  </action>
  <verify>
    <automated>grep "taxSlab" frontend/src/contexts/SettingsContext.tsx</automated>
  </verify>
  <done>Tax slab setting is integrated into the global settings state and persisted to localStorage.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Tax Slab UI and Table Updates</name>
  <files>frontend/src/App.tsx</files>
  <action>
    - Extract `taxSlab` and `setTaxSlab` from `useSettings()`.
    - Update `fetchSummary` to include `taxSlab` in the query parameters for the `/summary` API call.
    - In the Header, add an input (or small dropdown/labeled input) next to the performance toggle to adjust the "Tax Slab %".
    - Update the `data-table` in `App.tsx`:
      1. Add a new `<th>` for the "Post-Tax" metric.
      2. The header label should reactively display "Post-Tax XIRR" or "Post-Tax Return".
      3. Add a new `<td>` to display `folio.metrics.postTaxXirr` or `folio.metrics.postTaxAbsoluteReturn`.
      4. Use `formatPercent` for display.
      5. Add a Tooltip icon (from Lucide) to the header explaining "Post-Tax metric accounts for estimated tax liability on unrealized gains".
  </action>
  <verify>
    <automated>grep -E "postTaxXirr|postTaxAbsoluteReturn" frontend/src/App.tsx</automated>
  </verify>
  <done>The UI displays the new post-tax metrics and allows users to control the tax slab input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI → Client Storage | taxSlab is stored in localStorage. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Information Disclosure | localStorage | accept | taxSlab is a generic setting, not PII. |
</threat_model>

<verification>
Manual verification: Open the dashboard, change the tax slab, and verify the Post-Tax column updates after the API refetches.
</verification>

<success_criteria>
- Marginal Tax Slab is configurable via UI.
- Holdings table shows Post-Tax XIRR/Return.
- API requests include the configured tax slab.
</success_criteria>

<output>
Create `.planning/phases/08-advanced-metrics-post-tax-xirr/08-02-SUMMARY.md` when done
</output>
