---
phase: 14-ui-ux-refinement-polish
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified: [frontend/src/index.css, frontend/src/App.css, frontend/src/App.tsx, frontend/src/components/Dashboard/AddAssetModal.tsx, frontend/src/components/Tax/SimulationModal.tsx]
autonomous: true
requirements: [V4-FIX-02]

must_haves:
  truths:
    - "Input fields have high contrast and readable text in both light and dark modes"
    - "Badge contrast is sufficient in dark mode"
    - "All forms and modals use standardized semantic variables for inputs"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "Semantic input variables and global element styling"
    - path: "frontend/src/components/Dashboard/AddAssetModal.tsx"
      provides: "Refactored input styling using semantic variables"
---

<objective>
Standardize input styling and improve dark mode accessibility through semantic CSS variables and global contrast fixes.

Purpose: Fix "black text" issues in dark mode and ensure consistent accessibility across all forms and modals.
Output: High-contrast input elements and readable badges in dark mode.
</objective>

<execution_context>
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-ui-ux-refinement-polish/14-CONTEXT.md
@frontend/src/index.css
@frontend/src/App.css
@frontend/src/App.tsx
@frontend/src/components/Dashboard/AddAssetModal.tsx
@frontend/src/components/Tax/SimulationModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Semantic Input Variables</name>
  <files>frontend/src/index.css</files>
  <action>
    - Per D-04: Add `--input-bg`, `--input-border`, and `--input-text` variables to `:root` (light theme) and `[data-theme='dark']` blocks.
    - Light values: `--input-bg: #ffffff;`, `--input-border: #e2e8f0;`, `--input-text: #0f172a;`.
    - Dark values: `--input-bg: #1e293b;`, `--input-border: #334155;`, `--input-text: #f8fafc;`.
    - Add global styling for `input`, `select`, and `textarea` using these variables (ensure `color`, `background-color`, and `border` are set).
  </action>
  <verify>
    <automated>grep -c "\--input-bg" frontend/src/index.css</automated>
  </verify>
  <done>Semantic input variables are defined and applied globally in index.css.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Modals and Forms</name>
  <files>frontend/src/components/Dashboard/AddAssetModal.tsx, frontend/src/components/Tax/SimulationModal.tsx, frontend/src/App.tsx</files>
  <action>
    - Per D-05: Update all input-related elements in these files.
    - Remove hardcoded `background: 'var(--bg-secondary)'` from style props on `input` and `select` elements.
    - Ensure they inherit global styling or explicitly use the new `--input-*` variables.
    - Update the "Rename Member" and "Import CAS" modals in `App.tsx` to use the same pattern.
  </action>
  <verify>
    <automated>grep -v "var(--bg-secondary)" frontend/src/components/Dashboard/AddAssetModal.tsx | grep -c "input"</automated>
  </verify>
  <done>Modals are updated to use semantic input variables, ensuring high contrast in dark mode.</done>
</task>

<task type="auto">
  <name>Task 3: Fix Badge Contrast and Color Consistency</name>
  <files>frontend/src/App.css</files>
  <action>
    - Per D-06: Review and fix `badge-slab` contrast in dark mode.
    - Update `.badge-slab` in `App.css` to use a more readable color in dark mode (e.g., use `--text-secondary` and a slightly darker/lighter background if needed).
    - Ensure all `input` elements have `border-radius: 8px;` and `padding: 0.75rem;` for a polished look.
  </action>
  <verify>
    <automated>grep -c "\.badge-slab" frontend/src/App.css</automated>
  </verify>
  <done>Badges and input fields have consistent, accessible styling across themes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CSS Styling | Global styles affect all components |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02 | Tampering | frontend/src/index.css | mitigate | Use CSS variables to avoid hardcoded values |
| T-14-SC | Tampering | npm installs | accept | No new packages added |
</threat_model>

<verification>
1. Start the frontend: `npm run dev`.
2. Toggle between Light and Dark modes.
3. Open "Add Alternative Asset" modal and check input readability.
4. Open "Simulation" modal and check input readability.
5. Check the "Scheme Name" table and verify "Managed Profile" or "Type" badges are clearly readable.
</verification>

<success_criteria>
- Inputs in dark mode have light text on a dark background (no black text on dark bg).
- All form elements follow the same semantic variable theme.
- Badges have accessible contrast ratios.
</success_criteria>

<output>
Create `.planning/phases/14-ui-ux-refinement-polish/14-02-SUMMARY.md` when done
</output>
