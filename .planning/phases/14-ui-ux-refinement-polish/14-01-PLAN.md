---
phase: 14-ui-ux-refinement-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend/src/App.tsx, frontend/src/App.css]
autonomous: true
requirements: [V4-FIX-01]

must_haves:
  truths:
    - "Scheme Breakdown chart is visible with a height of at least 300px"
    - "XAxis and YAxis are visible on the Scheme Breakdown chart"
    - "Scheme names on XAxis are readable and not overlapping"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "Updated Recharts component with Axes enabled"
    - path: "frontend/src/App.css"
      provides: "Layout fix for chart container"
---

<objective>
Fix the non-rendering "Scheme Breakdown" chart by providing a mandatory height and enabling axis visibility for better data interpretation.

Purpose: Resolve a critical UI bug where users cannot see their asset distribution.
Output: A functional and readable bar chart showing scheme-wise valuation.
</objective>

<execution_context>
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-ui-ux-refinement-polish/14-CONTEXT.md
@frontend/src/App.tsx
@frontend/src/App.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Chart Container Height</name>
  <files>frontend/src/App.css, frontend/src/App.tsx</files>
  <action>
    - In `frontend/src/App.css`, define `.chart-container` with `height: 300px;` and `width: 100%;`.
    - In `frontend/src/App.tsx`, ensure the `ResponsiveContainer` wrapping the `BarChart` uses these dimensions (it already uses 100% width/height, but its parent needs the explicit height).
    - Per D-01: Use a mandatory height (300px is recommended).
  </action>
  <verify>
    <automated>grep -c "\.chart-container" frontend/src/App.css</automated>
  </verify>
  <done>Chart container has a fixed height, allowing ResponsiveContainer to render.</done>
</task>

<task type="auto">
  <name>Task 2: Enable Axes and Improve Readability</name>
  <files>frontend/src/App.tsx</files>
  <action>
    - In the `BarChart` inside `App.tsx`:
      - Remove `hide` prop from `XAxis` and `YAxis` (per D-02).
      - Add `tick={{ fontSize: 11 }}` to both axes.
      - Update `XAxis` to handle long scheme names (per D-03): add `interval={0}` and `angle={-45}` or `textAnchor="end"`, and set a `height={60}` for the axis to accommodate rotated labels.
      - Ensure `CartesianGrid` stroke uses `var(--border-color)`.
  </action>
  <verify>
    <automated>grep -v "hide" frontend/src/App.tsx | grep -c "XAxis"</automated>
  </verify>
  <done>Axes are visible and XAxis labels are rotated to prevent overlapping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client Render | Recharts rendering depends on data structure from portfolio summary |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | frontend/src/App.tsx | mitigate | Use standard Recharts components and props |
| T-14-SC | Tampering | npm installs | accept | No new packages added in this plan |
</threat_model>

<verification>
1. Start the frontend: `npm run dev` in `frontend/`.
2. Visit the dashboard and verify the "Scheme Breakdown" chart is visible.
3. Check that both X and Y axes are displayed.
4. Verify that labels on the X axis are readable even for long names.
</verification>

<success_criteria>
- Scheme Breakdown chart renders correctly with data.
- XAxis labels are rotated/formatted to avoid overlap.
- YAxis shows valuation scale.
</success_criteria>

<output>
Create `.planning/phases/14-ui-ux-refinement-polish/14-01-SUMMARY.md` when done
</output>
