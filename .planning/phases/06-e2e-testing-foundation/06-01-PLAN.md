---
phase: 06-e2e-testing-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend/package.json, frontend/playwright.config.ts, frontend/tests/smoke.spec.ts]
autonomous: true
requirements: [V2-E2E-01]

must_haves:
  truths:
    - "Playwright is installed as a devDependency in frontend/"
    - "Playwright is configured to run against Vite dev server"
    - "A smoke test passes by checking if the app loads"
  artifacts:
    - path: "frontend/playwright.config.ts"
      provides: "Playwright configuration"
    - path: "frontend/tests/smoke.spec.ts"
      provides: "Initial smoke test"
  key_links:
    - from: "frontend/playwright.config.ts"
      to: "localhost:5173"
      via: "webServer.url"
---

<objective>
Setup Playwright infrastructure in the frontend directory. This includes installing dependencies, configuring the environment for Vite, and creating a baseline smoke test.

Purpose: Provide the foundation for all E2E testing.
Output: Configured Playwright environment and passing smoke test.
</objective>

<execution_context>
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-e2e-testing-foundation/06-CONTEXT.md
@frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Playwright and dependencies</name>
  <files>frontend/package.json</files>
  <action>
    Install @playwright/test and @faker-js/faker as devDependencies in the frontend/ directory. 
    Initialize Playwright using `npx playwright install chromium --with-deps`.
  </action>
  <verify>
    <automated>cd frontend && npm list @playwright/test @faker-js/faker</automated>
  </verify>
  <done>Playwright and Faker are installed in the frontend project.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Playwright for Vite</name>
  <files>frontend/playwright.config.ts</files>
  <action>
    Create playwright.config.ts in frontend/. 
    - Set baseURL to http://localhost:5173 (per D-01, D-02).
    - Configure webServer to run 'npm run dev' on port 5173, reuseExistingServer: !process.env.CI (per D-03).
    - Limit browsers to 'chromium' only (per D-04).
    - Set testDir to './tests'.
  </action>
  <verify>
    <automated>test -f frontend/playwright.config.ts && grep "chromium" frontend/playwright.config.ts</automated>
  </verify>
  <done>Playwright is configured to work with the Vite development server.</done>
</task>

<task type="auto">
  <name>Task 3: Create initial smoke test</name>
  <files>frontend/tests/smoke.spec.ts</files>
  <action>
    Create a simple smoke test in frontend/tests/smoke.spec.ts that navigates to the baseURL and checks if the page title or a key element (like "Portfolio Tracker") is visible.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test tests/smoke.spec.ts</automated>
  </verify>
  <done>A basic smoke test passes against the local dev server.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test Runner → System | Playwright executes in local environment, potentially starting servers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | frontend/package.json | mitigate | Use exact versions for playwright and faker |
| T-06-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED] packages |
</threat_model>

<verification>
Run `npx playwright test tests/smoke.spec.ts` from the frontend directory.
</verification>

<success_criteria>
Playwright is fully functional, configured for the Vite dev server, and the smoke test passes.
</success_criteria>

<output>
Create `.planning/phases/06-e2e-testing-foundation/06-01-SUMMARY.md` when done
</output>
