---
phase: 06-e2e-testing-foundation
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified: [frontend/tests/cas-upload.spec.ts, frontend/tests/logout.spec.ts]
autonomous: true
requirements: [V2-E2E-02]

must_haves:
  truths:
    - "User can upload a CAS file and see mocked data reflected on the dashboard"
    - "UI handles upload errors gracefully with error messages"
    - "Logout flow clears the session and returns to login/welcome"
  artifacts:
    - path: "frontend/tests/cas-upload.spec.ts"
      provides: "E2E tests for CAS flow"
    - path: "frontend/tests/logout.spec.ts"
      provides: "E2E tests for logout"
  key_links:
    - from: "frontend/tests/cas-upload.spec.ts"
      to: "frontend/tests/utils/cas-mock.ts"
      via: "import"
    - from: "frontend/tests/cas-upload.spec.ts"
      to: "frontend/tests/pom/UploadPage.ts"
      via: "import"
---

<objective>
Implement the core End-to-End tests for the CAS upload flow and session management.

Purpose: Ensure the critical user path is functional and resilient to API errors.
Output: Completed E2E test suite for CAS upload and logout.
</objective>

<execution_context>
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-e2e-testing-foundation/06-02-SUMMARY.md
@frontend/tests/pom/UploadPage.ts
@frontend/tests/pom/DashboardPage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement CAS Upload Happy Path Test</name>
  <files>frontend/tests/cas-upload.spec.ts</files>
  <action>
    Create frontend/tests/cas-upload.spec.ts:
    - Initialize UploadPage and DashboardPage POMs.
    - Set up CAS mocking using the utility from Plan 02 (per D-05).
    - Navigate to the upload page.
    - Select a dummy PDF file (or create one on the fly).
    - Verify that the upload triggers the mock and navigates to the dashboard.
    - Verify that the dashboard displays the mocked investor info and folio details (per V2-E2E-02).
  </action>
  <verify>
    <automated>cd frontend && npx playwright test tests/cas-upload.spec.ts -g "happy path"</automated>
  </verify>
  <done>Happy path CAS upload E2E test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement CAS Upload Error Handling Test</name>
  <files>frontend/tests/cas-upload.spec.ts</files>
  <action>
    Add an error handling test case to frontend/tests/cas-upload.spec.ts:
    - Mock a 500 error for the upload endpoint (per D-08).
    - Trigger the upload flow.
    - Verify that an appropriate error message is displayed in the UI.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test tests/cas-upload.spec.ts -g "error handling"</automated>
  </verify>
  <done>Error handling E2E test passes.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Logout Flow Test</name>
  <files>frontend/tests/logout.spec.ts</files>
  <action>
    Create frontend/tests/logout.spec.ts:
    - Navigate to the dashboard.
    - Use the DashboardPage POM to click logout (per D-12).
    - Verify that the user is redirected away from the dashboard and session state is cleared.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test tests/logout.spec.ts</automated>
  </verify>
  <done>Logout flow E2E test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test Assets → Browser | Test runner controls browser to simulate user actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Denial of Service | Playwright Test Suite | accept | E2E tests run in controlled environments (local/CI) |
</threat_model>

<verification>
Run the full suite: `npx playwright test tests/cas-upload.spec.ts tests/logout.spec.ts`
</verification>

<success_criteria>
Core E2E flows (Upload Happy Path, Upload Error, and Logout) are fully tested and passing.
</success_criteria>

<output>
Create `.planning/phases/06-e2e-testing-foundation/06-03-SUMMARY.md` when done
</output>
