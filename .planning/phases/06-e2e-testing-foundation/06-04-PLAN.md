---
phase: 06-e2e-testing-foundation
plan: 04
type: execute
wave: 4
depends_on: ["06-03"]
files_modified: [.github/workflows/e2e.yml]
autonomous: true
requirements: [V2-E2E-01]

must_haves:
  truths:
    - "GitHub Actions workflow exists for E2E tests"
    - "Smoke test runs on every Pull Request"
    - "Full E2E suite runs on merge to main"
  artifacts:
    - path: ".github/workflows/e2e.yml"
      provides: "CI configuration for E2E tests"
---

<objective>
Integrate the E2E test suite into GitHub Actions to ensure continuous quality.

Purpose: Automate testing during the development lifecycle.
Output: Active GitHub Actions workflow for E2E testing.
</objective>

<execution_context>
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-e2e-testing-foundation/06-03-SUMMARY.md
@frontend/playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E GitHub Actions Workflow</name>
  <files>.github/workflows/e2e.yml</files>
  <action>
    Create .github/workflows/e2e.yml (per D-13):
    - Trigger on 'pull_request' and 'push' to 'main'.
    - Job 'e2e-tests':
      - Checkout code.
      - Setup Node.js.
      - Install dependencies (frontend and Playwright browsers).
      - Run smoke test ONLY on pull_request (per D-15).
      - Run full test suite ONLY on push to main (per D-14).
    - Ensure it uses the 'CI: true' environment variable for Playwright config consistency.
  </action>
  <verify>
    <automated>test -f .github/workflows/e2e.yml && grep "on:" .github/workflows/e2e.yml</automated>
  </verify>
  <done>E2E CI workflow is configured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>GitHub Actions workflow for E2E tests</what-built>
  <how-to-verify>
    Inspect .github/workflows/e2e.yml to ensure the logic for pull_request (smoke only) and main (full suite) is correct.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI Runner → Repository | GitHub Actions has access to repository code and potentially secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Information Disclosure | CI Logs | mitigate | Do not log sensitive env vars in CI console (per D-16) |
</threat_model>

<verification>
Check for existence of `.github/workflows/e2e.yml` and verify its YAML structure.
</verification>

<success_criteria>
GitHub Actions workflow is correctly defined and ready to trigger on PRs and merges.
</success_criteria>

<output>
Create `.planning/phases/06-e2e-testing-foundation/06-04-SUMMARY.md` when done
</output>
