---
phase: 06-e2e-testing-foundation
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [frontend/tests/utils/cas-mock.ts, frontend/tests/pom/DashboardPage.ts, frontend/tests/pom/UploadPage.ts]
autonomous: true
requirements: [V2-E2E-01]

must_haves:
  truths:
    - "Network mocking utility exists and uses MockCASGenerator"
    - "Page Object Models exist for Dashboard and Upload pages"
  artifacts:
    - path: "frontend/tests/utils/cas-mock.ts"
      provides: "CAS API mocking logic"
    - path: "frontend/tests/pom/DashboardPage.ts"
      provides: "Dashboard POM"
    - path: "frontend/tests/pom/UploadPage.ts"
      provides: "Upload POM"
  key_links:
    - from: "frontend/tests/utils/cas-mock.ts"
      to: "backend/test-utils/MockCASGenerator.ts"
      via: "import"
---

<objective>
Implement the mocking infrastructure and Page Object Models (POMs) to enable clean, isolated E2E tests.

Purpose: Isolate frontend tests from backend state and promote test maintainability.
Output: Reusable mocking utilities and POMs.
</objective>

<execution_context>
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-e2e-testing-foundation/06-01-SUMMARY.md
@backend/test-utils/MockCASGenerator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CAS Mocking Utility</name>
  <files>frontend/tests/utils/cas-mock.ts</files>
  <action>
    Create a utility frontend/tests/utils/cas-mock.ts that:
    - Imports MockCASGenerator from ../../../backend/test-utils/MockCASGenerator (per D-06).
    - Exports a function `mockCASUpload(page: Page, mockData?: MockCAS)` that uses `page.route` to intercept POST /api/portfolio/upload and returns the mocked JSON response (per D-05, D-07).
    - Includes a utility to mock API error states (4xx/5xx) (per D-08).
  </action>
  <verify>
    <automated>test -f frontend/tests/utils/cas-mock.ts && grep "page.route" frontend/tests/utils/cas-mock.ts</automated>
  </verify>
  <done>CAS mocking utility is implemented and ready for use in tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Page Object Models</name>
  <files>frontend/tests/pom/DashboardPage.ts, frontend/tests/pom/UploadPage.ts</files>
  <action>
    Create Page Object Models:
    - UploadPage.ts: Methods for uploading a file, checking upload status, and handling errors.
    - DashboardPage.ts: Methods for verifying portfolio summary, holdings table, and logout (per D-12).
    - Ensure both use 'locators' effectively for resilient tests.
  </action>
  <verify>
    <automated>test -f frontend/tests/pom/DashboardPage.ts && test -f frontend/tests/pom/UploadPage.ts</automated>
  </verify>
  <done>POMs for the core user flow are implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Global Setup for Auth</name>
  <files>frontend/tests/utils/auth-setup.ts</files>
  <action>
    Implement a minimal global setup/teardown boilerplate in frontend/tests/utils/auth-setup.ts to inject a default "test user" session (per D-09, D-10). For now, this can just be a mock route for /api/auth/me or similar.
  </action>
  <verify>
    <automated>test -f frontend/tests/utils/auth-setup.ts</automated>
  </verify>
  <done>Auth setup boilerplate is created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mock Data → Frontend | Mocked API responses simulate backend behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Information Disclosure | frontend/tests/utils/cas-mock.ts | accept | Mock data is generated randomly, no real investor data used |
</threat_model>

<verification>
Check for existence of files and verify that MockCASGenerator is correctly imported.
</verification>

<success_criteria>
Mocking utilities and POMs are implemented and pass basic lint/type checks.
</success_criteria>

<output>
Create `.planning/phases/06-e2e-testing-foundation/06-02-SUMMARY.md` when done
</output>
