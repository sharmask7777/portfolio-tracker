---
phase: 16-persistent-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified: [docker-compose.yml, .env.example]
autonomous: true
requirements: [PERS-02, PERS-03]

must_haves:
  truths:
    - "PostgreSQL service has a healthcheck using pg_isready"
    - "Redis service has a healthcheck using redis-cli ping"
    - "Backend service waits for postgres and redis to be healthy before starting"
    - "Docker Compose uses variables from a root .env file"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Health-aware orchestration"
      contains: ["healthcheck:", "pg_isready", "redis-cli ping", "condition: service_healthy"]
    - path: ".env.example"
      provides: "Template for environment configuration"
  key_links:
    - from: "backend"
      to: "postgres"
      via: "depends_on condition"
---

<objective>
Implement healthchecks and dependency management in Docker Compose, and establish environment-based configuration using .env files.

Purpose: Improve reliability by ensuring services start in the correct order and making the configuration easily manageable and secure.
Output: Updated docker-compose.yml with healthchecks and dependency conditions; new root .env.example.
</objective>

<execution_context>
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.antigravitycli/get-shit-done/workflows/execute-plan.md
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.antigravitycli/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docker-compose.yml
@backend/.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Healthchecks to Database Services</name>
  <files>docker-compose.yml</files>
  <action>
    Add `healthcheck` configurations to `postgres` and `redis` services:
    - `postgres`: Use `pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-portfolio_tracker}`.
    - `redis`: Use `redis-cli ping`.
    Set appropriate `interval`, `timeout`, `retries`, and `start_period`.
  </action>
  <verify>
    grep -A 10 "healthcheck:" docker-compose.yml
  </verify>
  <done>Healthchecks added to database services.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Service Dependencies</name>
  <files>docker-compose.yml</files>
  <action>
    Update the `backend` service in `docker-compose.yml` to use `depends_on`:
    - Add `depends_on` block for `postgres` and `redis`.
    - Use `condition: service_healthy` for both dependencies.
  </action>
  <verify>
    grep -A 5 "depends_on:" docker-compose.yml | grep "condition: service_healthy"
  </verify>
  <done>Backend service now depends on healthy database services.</done>
</task>

<task type="auto">
  <name>Task 3: Establish Environment Configuration</name>
  <files>docker-compose.yml, .env.example</files>
  <action>
    1. Create a root `.env.example` file aggregating variables from `backend/.env.example` and adding `VITE_API_URL` for the frontend.
    2. Update `docker-compose.yml` to use these environment variables for:
       - Database credentials (POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB).
       - Connection strings (DATABASE_URL, REDIS_URL).
       - Ports and hostnames.
    Ensure `DATABASE_URL` uses the service name `postgres` instead of `localhost` when used within the Docker network.
  </action>
  <verify>
    test -f .env.example && grep "\${" docker-compose.yml
  </verify>
  <done>Configuration managed via .env variables in Docker Compose.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env -> Docker Compose | Secret injection point |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Information Disclosure | .env files | mitigate | Ensure .env is in .gitignore (check root .gitignore) |
| T-16-04 | Improper Exception Handling | Healthchecks | mitigate | Configure realistic timeouts and retries to avoid flapping |
</threat_model>

<verification>
1. `docker-compose config` validates the file with .env variables.
2. `cp .env.example .env` and `docker-compose up -d`.
3. `docker-compose ps` shows services as "(healthy)".
</verification>

<success_criteria>
- Backend only starts after DB and Redis are healthy.
- Configuration is centralized and easily overridable via .env.
</success_criteria>

<output>
Create `.planning/phases/16-persistent-infrastructure/16-02-SUMMARY.md` when done
</output>
