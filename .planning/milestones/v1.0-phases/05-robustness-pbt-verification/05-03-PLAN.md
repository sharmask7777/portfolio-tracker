---
phase: 05-robustness-pbt-verification
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [backend/src/services/tax.pbt.spec.ts]
autonomous: true
requirements: [PBT-TAX]

must_haves:
  truths:
    - "Taxable gains never exceed raw realized gains (taxable <= raw)"
    - "Loss set-off rules are strictly followed (LTCL only sets off LTCG, STCL sets off STCG then LTCG)"
    - "FIFO (First-In, First-Out) lot depletion is maintained across multiple buy/sell cycles"
  artifacts:
    - path: "backend/src/services/tax.pbt.spec.ts"
      provides: "Verification of Indian Capital Gains tax logic"
---

<objective>
Verify exact tax loss set-off invariants and FIFO lot depletion logic using Property-Based Testing.

Purpose: Guarantee the accuracy of tax calculations, which is critical for user trust.
Output: Comprehensive PBT suite for TaxService.
</objective>

<execution_context>
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/src/services/tax.service.ts
@backend/src/test/arbitraries.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify Tax Loss Set-off Invariants</name>
  <files>backend/src/services/tax.pbt.spec.ts</files>
  <behavior>
    - Property 1: `TaxableLTCG = max(0, RealizedLTCG - RealizedLTCL - max(0, RealizedSTCL - RealizedSTCG))`
    - Property 2: `TaxableSTCG = max(0, RealizedSTCG - RealizedSTCL)`
    - Property 3: `TotalTaxableGain <= TotalRealizedGain` (sum of taxable <= sum of realized).
  </behavior>
  <action>
    Create `backend/src/services/tax.pbt.spec.ts`.
    Generate sequences of transactions that result in various combinations of STCG, LTCG, STCL, and LTCL.
    Verify that the `calculatePortfolioTax` result correctly implements the set-off rules for both Equity and Debt assets.
  </action>
  <verify>
    <automated>npm test backend/src/services/tax.pbt.spec.ts</automated>
  </verify>
  <done>Tax set-off rules are verified and correct.</done>
</task>

<task type="auto">
  <name>Task 2: Verify FIFO Depletion and Grandfathering</name>
  <files>backend/src/services/tax.pbt.spec.ts</files>
  <action>
    Add properties to verify:
    - FIFO: Ensure that if a user sells X units, the oldest X units across all buy lots are the ones used for gain calculation.
    - Grandfathering (Jan 2018): Verify that for assets bought before Jan 31, 2018, the cost acquisition is adjusted correctly based on the Fair Market Value (FMV) and Selling Price.
    - Budget 2024 Transition: Verify that the 12.5% LTCG rate applies only to sales after July 23, 2024.
  </action>
  <verify>
    <automated>npm test backend/src/services/tax.pbt.spec.ts</automated>
  </verify>
  <done>FIFO and historical tax rules are accurately implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Data -> Tax Engine | Tax calculation must be robust against zero-cost bonus units and penny-stock extreme values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | TaxService | accept | Logic verification only, no PII handled in service. |
</threat_model>

<verification>
Run the tax PBT suite and ensure all invariants pass across 1000+ random portfolios.
</verification>

<success_criteria>
Tax engine is mathematically verified against Indian tax laws.
</success_criteria>

<output>
Create `.planning/phases/05-robustness-pbt-verification/05-03-SUMMARY.md` when done
</output>
