---
phase: 05-robustness-pbt-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/test-utils/MockCASGenerator.ts, backend/src/test/arbitraries.ts]
autonomous: true
requirements: [PBT-GEN, PBT-ARB]

must_haves:
  truths:
    - "MockCASGenerator produces realistic names, PANs, and Folio numbers using faker"
    - "Generator supports edge cases like BONUS units (0 nav) and SWITCH_IN/OUT transactions"
    - "fast-check arbitraries generate valid and varied CAS JSON structures"
  artifacts:
    - path: "backend/test-utils/MockCASGenerator.ts"
      provides: "High-fidelity synthetic data generation"
    - path: "backend/src/test/arbitraries.ts"
      provides: "Shared PBT primitives for the test suite"
---

<objective>
Build a robust engine to generate thousands of diverse, realistic CAMS/KFintech CAS JSON statements for stress-testing the system, and establish the PBT primitive layer.

Purpose: Provide a limitless supply of valid and edge-case CAS data to verify logic without relying on static mocks.
Output: Upgraded MockCASGenerator and new shared arbitraries file.
</objective>

<execution_context>
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/test-utils/MockCASGenerator.ts
@backend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden MockCASGenerator with Faker and Edge Cases</name>
  <files>backend/test-utils/MockCASGenerator.ts</files>
  <action>
    Upgrade MockCASGenerator to use `@faker-js/faker` for all string fields (Investor Name, Email, Address, PAN, Folio, AMC name).
    Expand the transaction generation logic to support:
    - `BONUS` types: units > 0, nav = 0, amount = 0.
    - `DIVIDEND_REINVESTMENT`: units > 0, nav > 0, amount > 0.
    - `SWITCH_IN` / `SWITCH_OUT` pairs.
    - Extreme numeric values: very small fractional units (0.001), very large NAVs (1,000,000+).
    - Chronological consistency: Ensure transaction dates are sorted and balanced.
  </action>
  <verify>
    <automated>npx ts-node -e "import { MockCASGenerator } from './backend/test-utils/MockCASGenerator'; console.log(JSON.stringify(MockCASGenerator.generate({numFolios:2, numSchemesPerFolio:2}), null, 2))"</automated>
  </verify>
  <done>Generator produces varied, realistic JSON with edge case transaction types.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Shared fast-check Arbitraries</name>
  <files>backend/src/test/arbitraries.ts</files>
  <action>
    Create a new file `backend/src/test/arbitraries.ts` that exports `fast-check` (fc) arbitraries for:
    - `arbitraryTransaction`: Generates valid transaction objects with realistic dates and amounts.
    - `arbitraryScheme`: Generates a scheme with a sequence of transactions.
    - `arbitraryCAS`: A full CAS structure generator building on the above.
    - Use `fc.pre()` or filtering to ensure that 'SELL' transactions only occur if there are sufficient 'BUY' units beforehand (FIFO/balance sanity).
  </action>
  <verify>
    <automated>npx ts-node -e "import * as fc from 'fast-check'; import { arbitraryCAS } from './backend/src/test/arbitraries'; fc.assert(fc.property(arbitraryCAS, (cas) => cas.folios.length >= 0))"</automated>
  </verify>
  <done>Reusable arbitraries for CAS data are available for PBT tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test Data -> Logic | Synthetic data must not be so malformed that it causes unhandled exceptions (unless that's the test case). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | MockCASGenerator | accept | Internal tool, low risk. |
</threat_model>

<verification>
Verify that `MockCASGenerator` can generate a complex portfolio with 5 folios and 10 schemes each without error.
</verification>

<success_criteria>
Mock generator is high-fidelity and shared arbitraries are defined.
</success_criteria>

<output>
Create `.planning/phases/05-robustness-pbt-verification/05-01-SUMMARY.md` when done
</output>
