---
phase: 05-robustness-pbt-verification
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [backend/src/services/performance.pbt.spec.ts]
autonomous: true
requirements: [PBT-XIRR]

must_haves:
  truths:
    - "XIRR calculation is stable (no NaN/Infinity) for extreme cash flow sequences"
    - "Monotonicity property holds: increasing the final portfolio value always results in equal or higher XIRR"
    - "The 30-day heuristic correctly caps annualization volatility for short-term holdings"
  artifacts:
    - path: "backend/src/services/performance.pbt.spec.ts"
      provides: "Mathematical verification of performance metrics"
---

<objective>
Verify XIRR stability and exactness across extreme cash flow scenarios using Property-Based Testing.

Purpose: Ensure institutional-grade reliability of core performance metrics.
Output: Comprehensive PBT suite for PerformanceService.
</objective>

<execution_context>
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.antigravitycli/get-shit-done/workflows/execute-plan.md
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.antigravitycli/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/src/services/performance.service.ts
@backend/src/test/arbitraries.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement XIRR Stability and Invariant Tests</name>
  <files>backend/src/services/performance.pbt.spec.ts</files>
  <behavior>
    - Stability: Any sequence of cash flows (within realistic limits) must produce a finite number.
    - Identity: 0% net gain (final value = total net investment) should return approx 0% XIRR.
    - Monotonicity: If `v1 > v2`, then `XIRR(flows, v1) >= XIRR(flows, v2)`.
    - Sign Sensitivity: XIRR requires at least one positive and one negative flow; check fallback to 0.
  </behavior>
  <action>
    Create `backend/src/services/performance.pbt.spec.ts` using `fast-check` and `arbitraryTransaction`.
    Test the `calculateXIRR` method with at least 1000 runs per property.
    Verify stability with "hostile" inputs: very large transactions (10B+), very small transactions (0.01), and rapid buy/sell cycles.
  </action>
  <verify>
    <automated>npm test backend/src/services/performance.pbt.spec.ts</automated>
  </verify>
  <done>XIRR invariants are verified and stable.</done>
</task>

<task type="auto">
  <name>Task 2: Verify XIRR Heuristics and Capping</name>
  <files>backend/src/services/performance.pbt.spec.ts</files>
  <action>
    Add tests to verify:
    - The < 30 days heuristic: Ensure XIRR returns a value comparable to Absolute Return for short periods instead of astronomical annualized numbers.
    - The 1000% cap: Ensure XIRR never exceeds 10.0 (1000%) or drops below -1.0 (-100%).
    - Volatility check: Ensure the solver doesn't hang on edge cases like all-positive or all-negative flows.
  </action>
  <verify>
    <automated>npm test backend/src/services/performance.pbt.spec.ts</automated>
  </verify>
  <done>Boundary conditions and heuristics are mathematically sound.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Solver -> Application | The node-irr solver is an external dependency; we must ensure its failure modes are handled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Denial of Service | PerformanceService | mitigate | PBT ensures no infinite loops or hangs in the solver for edge case data. |
</threat_model>

<verification>
Run the full PBT suite for performance metrics and ensure 0 failures.
</verification>

<success_criteria>
Performance metrics are verified against core mathematical invariants.
</success_criteria>

<output>
Create `.planning/phases/05-robustness-pbt-verification/05-02-SUMMARY.md` when done
</output>
