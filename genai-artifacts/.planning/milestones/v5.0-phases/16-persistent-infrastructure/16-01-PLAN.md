---
phase: 16-persistent-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docker-compose.yml]
autonomous: true
requirements: [PERS-01, DIST-03]

must_haves:
  truths:
    - "docker-compose.yml includes postgres, redis, backend, and frontend services"
    - "PostgreSQL data persists across container restarts using a named volume"
    - "Redis data persists across container restarts using a named volume"
    - "All services are configured to restart automatically"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Full stack orchestration"
      contains: ["services:", "postgres:", "redis:", "backend:", "frontend:", "volumes:"]
  key_links:
    - from: "backend"
      to: "postgres"
      via: "docker network"
    - from: "backend"
      to: "redis"
      via: "docker network"
---

<objective>
Refine docker-compose.yml to include all application services and implement persistent named volumes for data storage.

Purpose: Ensure the entire stack can be orchestrated together and that data is not lost when containers are removed.
Output: Updated docker-compose.yml with 4 services and 2 named volumes.
</objective>

<execution_context>
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.antigravitycli/get-shit-done/workflows/execute-plan.md
@/Users/shaleensharma/workplace/gemini_ws/portfolio-tracker/.antigravitycli/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@backend/Dockerfile
@frontend/Dockerfile
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Backend and Frontend services to docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <action>
    Update `docker-compose.yml` to include the `backend` and `frontend` services:
    - `backend`:
      - Build from `./backend`.
      - Map port 3001:3001.
      - Set environment variables (for now, use defaults or service names: DB_HOST=postgres, REDIS_HOST=redis).
      - Set `restart: always`.
    - `frontend`:
      - Build from `./frontend`.
      - Map port 80:80 (or 3000:80 if preferred, but requirement says "production-like", so 80 is standard).
      - Set `restart: always`.
  </action>
  <verify>
    grep -E "backend:|frontend:" docker-compose.yml
  </verify>
  <done>docker-compose.yml contains all 4 services.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Named Volumes for Postgres and Redis</name>
  <files>docker-compose.yml</files>
  <action>
    Update `volumes` section in `docker-compose.yml`:
    - Ensure `postgres_data` exists and is mounted to `/var/lib/postgresql/data` in the `postgres` service.
    - Add `redis_data` volume and mount it to `/data` in the `redis` service.
    - Set `restart: always` for `postgres` and `redis` services.
  </action>
  <verify>
    grep -E "postgres_data:|redis_data:" docker-compose.yml
  </verify>
  <done>Named volumes configured for both database services.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host -> Docker Volumes | Data persistence on host filesystem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure | Volumes | mitigate | Ensure host volume permissions are restrictive (handled by Docker) |
| T-16-02 | Denial of Service | Services | mitigate | `restart: always` to recover from crashes |
</threat_model>

<verification>
1. `docker-compose config` validates the file.
2. `docker-compose up -d` starts all 4 services.
3. `docker volume ls` shows `postgres_data` and `redis_data`.
</verification>

<success_criteria>
- All services start correctly.
- Volumes are created and attached.
</success_criteria>

<output>
Create `.planning/phases/16-persistent-infrastructure/16-01-SUMMARY.md` when done
</output>
